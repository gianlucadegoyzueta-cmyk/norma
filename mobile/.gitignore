# Dipendenze
/node_modules

# Progetti nativi generati da `npx cap add ios/android`.
# Vengono rigenerati sul Mac; non li versioniamo per non appesantire il repo e
# perché contengono path/config macchina-specifici. Se in futuro servisse
# committarli (es. per personalizzazioni native), rimuovere queste righe.
/ios
/android

# Output generazione asset (ricreabili con `npm run assets:generate`)
/assets/generated

# Ambiente / log
.env
.env*.local
*.log
.DS_Store

# Segreti di firma e build store — MAI nel repo
/build
google-services.json
*.jks
*.keystore
*.p8
*.p12
*.mobileprovision
fastlane/*.json
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output
Gemfile.lock
