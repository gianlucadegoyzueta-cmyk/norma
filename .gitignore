# dipendenze
/node_modules

# build Next.js
/.next
/out
/build

# variabili d'ambiente (NB: .env.example resta tracciato)
.env
.env*.local

# store locale dei segreti (cifrato, ma non va in git)
/.localsecrets

# TypeScript / Next
next-env.d.ts
*.tsbuildinfo

# sistema / editor
.DS_Store
*.log
npm-debug.log*
.vercel

# Gate #0 — artefatti diagnostici Ricevuta PDF (live, non versionati)
/tmp/gate0-ricevuta/
# config locale di Claude Code (non versionata)
# Claude: comandi condivisi versionati, config locale no
.claude/settings.local.json
supabase/.temp/
